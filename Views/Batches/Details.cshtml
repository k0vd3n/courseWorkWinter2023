@model WebApplication01.Models.Batche

@{
    ViewData["Title"] = "Details";
}

<h1>Подробности</h1>

<div>
    <h4>Партии</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.BatchSize)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.BatchSize)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.BatchCost)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.BatchCost)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.DateCostChange)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DateCostChange)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("OrderManager") || User.IsInRole("PurchaseManager") || User.IsInRole("Admin"))
    {
        <a asp-action="Edit" asp-route-id="@Model?.Id">Редактировать</a>
    }
    <a asp-action="Index">Обратно к списку</a>
</div>
